worker_processes  4;
error_log  /var/log/nginx/error.log notice;
pid        /var/run/nginx.pid;
worker_rlimit_nofile 1047552;
worker_shutdown_timeout 240s ;

events {
	multi_accept        on;
	worker_connections  16384;
	use                 epoll;
}

http {
    # {{/* Dynamic Server Blocks Placeholder */}}
	{{ template "servers" }}
}